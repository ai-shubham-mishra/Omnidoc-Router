# FastAPI and ASGI server
fastapi
uvicorn[standard]
python-multipart

# Data validation
pydantic

# Environment and config
python-dotenv

# HTTP client (for calling AgenticAPI)
httpx

# MongoDB
pymongo

# Redis (for session caching)
redis[hiredis]
aioredis

# Azure OpenAI (replacing Gemini)
openai>=1.0.0

# JWT authentication
PyJWT

# Azure Blob Storage
azure-storage-blob>=12.19.0

# Azure Key Vault (for secrets)
azure-identity
azure-keyvault-secrets

# Pinecone Vector Database
pinecone

# Text splitting for embeddings
tiktoken

# Text extraction from files
PyPDF2
python-docx
openai
marko